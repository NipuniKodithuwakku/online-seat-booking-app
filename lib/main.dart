import 'package:flutter/material.dart';
import './src/app.dart';

void main() {
  runApp(MaterialApp(
    title: "firebase authentication",
    debugShowCheckedModeBanner: false,
    home: new App(),
  ));
}
